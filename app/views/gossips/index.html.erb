<h1>Bienvenu sur notre super site de potin</h1>

<p>
<%@gossips.each do |gossip|%>
	<br>
	Auteur :
	  <% user = gossip.user_id %>
      <%=  @user.find(user).first_name %> <br> 
	Titre:
	<%=  gossip.title %> <%= link_to "View more...", controller:"gossips", action:"show", id: gossip.id %><br>
	
<%end%>

</p>

